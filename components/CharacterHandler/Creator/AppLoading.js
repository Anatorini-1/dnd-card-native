import React from "react";
import { StyleSheet, Text, View } from "react-native";

const AppLoading = () => {
  return (
    <View>
      <Text>App Loading (Placeholder)</Text>
    </View>
  );
};

export default AppLoading;

const styles = StyleSheet.create({});
